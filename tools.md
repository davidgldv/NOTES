##COMANDOS:

```
Comando head
Comando head
Comando head
Siempre alerta estas
```

ls = List files
cd = Change directory
mkdir = make dir. 
rm= remove
rmdir: remover dir. (solo si esta vacio)
rm -rf (*r*ecursivo *f*orce)
cat =concatenar

